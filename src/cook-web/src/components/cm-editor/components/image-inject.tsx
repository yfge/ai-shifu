/** inject image to mc-editor */
import React, { useContext } from 'react'

type ImageInjectProps = {
}

const ImageInject: React.FC<ImageInjectProps> = ({  }) => {
  
  return (
    <div>
        图片上传的组件接入到这个组件
    </div>
  )
}

export default ImageInject
